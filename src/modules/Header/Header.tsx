import { Container } from "@mantine/core";

import Logo from "@/components/Logo";

export default function Header() {
    return (
        <Container>
            <Logo />
        </Container>
    );
}